# Screenshots

Drop demo media here and reference it from the top-level `README.md` (see the
commented block in the **Demo** section). Suggested shots:

- `menubar.png` — the menu-bar icon (🎙) with its menu open.
- `setup.png` — the first-run setup window (API key + permissions).
- `demo.gif` — a short clip of dictating into an app, with the floating dot in
  frame so the recording state is visible.

Capture tips are in the top-level README's Demo section.
